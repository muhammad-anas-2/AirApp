<%= form_with(model: hostel) do |form| %>
  <% if hostel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hostel.errors.count, "error") %> prohibited this hostel from being saved:</h2>

      <ul>
        <% hostel.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :h_name %>
    <%= form.text_field :h_name %>
  </div>

  <div class="field">
    <%= form.label :h_phone %>
    <%= form.text_field :h_phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
